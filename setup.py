from setuptools import setup, find_packages

setup(
    name='ros_perception',
    version='0.1.0',
    install_requires=[
        'tensorflow',
        'matplotlib',
        'scikit-image',
        'numpy',
        ]
)
